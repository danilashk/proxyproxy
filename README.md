# URL Proxy Service

Прокси-сервис для создания коротких ссылок с проксированием контента. Позволяет создавать прокси-ссылки для любых URL и показывать их контент через ваш домен.

## Возможности

- 🔗 Создание коротких прокси-ссылок для любых URL
- 🌐 Полное проксирование HTML контента
- 🎨 Автоматическое проксирование CSS, JS, изображений и других ресурсов
- 🔄 Переписывание ссылок для корректной работы через прокси
- 📊 Счетчик посещений для каждой прокси-ссылки
- 💾 In-memory хранилище (легко заменить на Redis/БД)

## Как это работает

1. Вы вводите полный URL (например, `https://www.sports.ru/special/3x3/game-with-friend?roomId=SnvJ0621X`)
2. Сервис создает короткую прокси-ссылку (например, `ваш-домен.com/p/abc123`)
3. При переходе по прокси-ссылке:
   - Контент загружается с оригинального URL
   - Все ссылки и ресурсы переписываются для работы через прокси
   - Пользователь видит контент, но URL остается вашим доменом

## Структура проекта

```
/pages
  /index.js              - Главная страница с формой создания прокси-ссылок
  /api
    /create-proxy.js     - API для создания новых прокси-ссылок
    /proxy.js            - API для проксирования ресурсов (CSS, JS, изображения)
  /p
    /[id].js             - Динамический маршрут для показа проксированного контента
/lib
  /storage.js            - In-memory хранилище для прокси-ссылок
```

## Локальная разработка

```bash
npm install
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## Деплой на Vercel

1. **Fork этот репозиторий**
2. **Импортируйте проект в Vercel**:
   - Зайдите на [vercel.com](https://vercel.com)
   - Нажмите "New Project"
   - Импортируйте ваш GitHub репозиторий
3. **Деплой**:
   - Vercel автоматически определит настройки Next.js
   - Нажмите "Deploy"

## Использование

### Создание прокси-ссылки

1. Откройте главную страницу
2. Вставьте URL в поле ввода
3. Нажмите "Создать прокси-ссылку"
4. Скопируйте полученную ссылку

### API эндпоинты

**POST /api/create-proxy**
```json
{
  "url": "https://example.com/page"
}
```

Ответ:
```json
{
  "success": true,
  "proxyUrl": "https://your-domain.com/p/abc123",
  "id": "abc123",
  "originalUrl": "https://example.com/page"
}
```

**GET /p/[id]**
- Показывает проксированный контент

**GET /api/proxy?url=[encoded_url]**
- Проксирует отдельные ресурсы (CSS, JS, изображения)

## Ограничения текущей версии

⚠️ **In-memory хранилище**: Все ссылки хранятся в памяти и теряются при перезапуске сервера. Для продакшена рекомендуется:
- Redis
- MongoDB
- PostgreSQL
- Или другая база данных

## Улучшения для продакшена

- [ ] Замена in-memory хранилища на постоянное (Redis/БД)
- [ ] Добавление аутентификации
- [ ] Rate limiting
- [ ] Кеширование контента
- [ ] Обработка JavaScript-редиректов
- [ ] Поддержка WebSocket
- [ ] Логирование и мониторинг

## Безопасность

- Все запросы проксируются через серверную часть Next.js
- Оригинальные URL скрыты от конечных пользователей
- Нет прямого доступа к целевым серверам с клиента

## Лицензия

MIT